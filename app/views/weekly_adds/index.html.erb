<%- model_class = WeeklyAdd -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>

<div class ="container">
<div class ="row">
	<div class ="span4">
	<table class="table table-striped table-bordered">
		  <!--<thead>
		    <tr>
		      <th><%= model_class.human_attribute_name(:id) %></th>
		      <th><%= model_class.human_attribute_name(:WeeklyAddName) %></th>
		      
		      <th><%= model_class.human_attribute_name(:targeturl)%></th>
		      <th><%= model_class.human_attribute_name(:WeeklyAddID) %></th>
		      <th><%= model_class.human_attribute_name(:WeeklyAddType) %></th>
		      <th><%= model_class.human_attribute_name(:IsWeeklyFavorite) %></th>
		      <th><%= model_class.human_attribute_name(:created_at) %></th>
		      <th><%=t '.actions', :default => t("helpers.actions") %></th>
		    </tr>
		  </thead>-->
  		<tbody>
  			<%i = 0%>
		<% @weekly_adds.each do |weekly_add| %>
			<% if weekly_add.WeeklyAddID ==1%>	 
		       <tr>
				<%if can? :update, weekly_add%>
		      		<td><%= link_to weekly_add.id, weekly_add_path(weekly_add) %></td>
		      	<%end%>
		      	
			      	<td><%= i = i + 1%></td>
			        <td><%= weekly_add.WeeklyAddName %></td>
					<td><%=  link_to('Lets Go!', url_with_protocol(weekly_add.targeturl),:target => :blank, :class => 'btn btn-success')%></td>
			        <!--<td><%= weekly_add.WeeklyAddID %></td>
			        <td><%= weekly_add.WeeklyAddType %></td>
			        <td><%= weekly_add.IsWeeklyFavorite %></td>
			        <td><%=l weekly_add.created_at %></td>-->
					<%if can? :update, weekly_add%>
			        <td>
			          <%= link_to t('.edit', :default => t("helpers.links.edit")),
			                      edit_weekly_add_path(weekly_add), :class => 'btn btn-mini' %>
			          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
			                      weekly_add_path(weekly_add),
			                      :method => :delete,
			                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
			                      :class => 'btn btn-mini btn-danger' %>
			         <%end%>
			        </td>
			    </tr>
			<%end%>
			<%end%>	      
			</tbody>
			</table>
		</div>    
		
		<div class ="span4 offset1">
	      <table class="table table-striped table-bordered">
	      	<tbody>
  			<% j = 0%>
				<% @weekly_adds.each do |weekly_add| %>	
					<% if weekly_add.WeeklyAddID == 2 %>
			 			<tr>	
							<%if can? :update, weekly_add%>
		      				<td><%= link_to weekly_add.id, weekly_add_path(weekly_add) %></td>
		      				<%end%>
		      	
			      			<td><%= j = j + 1 %></td>
			        		<td><%= weekly_add.WeeklyAddName %></td>
							<td><%=  link_to('Lets Go!', url_with_protocol(weekly_add.targeturl),:target => :blank, :class => 'btn btn-success')%></td>
			        <!--<td><%= weekly_add.WeeklyAddID %></td>
			        <td><%= weekly_add.WeeklyAddType %></td>
			        <td><%= weekly_add.IsWeeklyFavorite %></td>
			        <td><%=l weekly_add.created_at %></td>-->
					<%if can? :update, weekly_add%>
			        <td>
			          <%= link_to t('.edit', :default => t("helpers.links.edit")),
			                      edit_weekly_add_path(weekly_add), :class => 'btn btn-mini' %>
			          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
			                      weekly_add_path(weekly_add),
			                      :method => :delete,
			                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
			                      :class => 'btn btn-mini btn-danger' %>
			         <%end%>
			        </td>
			        </tr>
		        <%end%>
		  <%end%>
	  	 </tbody>
      	</table>
      	</div>
	</div>
</div>
<!--<div class ="container">
	<%= link_to t('.new', :default => t("helpers.links.new")),new_weekly_add_path,
            :class => 'btn btn-primary' %>
		</div>-->


